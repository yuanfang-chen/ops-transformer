From c979b855797df8d412ca7e9292917f1627e6d886 Mon Sep 17 00:00:00 2001
From: ranzherui <ranzherui@huawei.com>
Date: Tue, 3 Mar 2026 09:00:00 +0800
Subject: [PATCH 1/4] GatherPaKvCacheNd: rename typename T to T_KEY

---
 .../op_kernel/arch35/gather_pa_kv_cache_nd.h  | 60 +++++++++----------
 1 file changed, 30 insertions(+), 30 deletions(-)

diff --git a/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h b/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
index c566f0c5..6a8db6b5 100644
--- a/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
+++ b/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
@@ -24,7 +24,7 @@ using namespace AscendC;
 constexpr uint32_t BLOCK_SIZE = 32;
 constexpr uint32_t DOUBLE_BUFFER = 2;
 
-template <typename T, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
 class GatherPaKvCacheNd {
 public:
     __aicore__ inline GatherPaKvCacheNd(TPipe *pipe, const GatherPaKvCacheTilingDataV35 *__restrict tiling)
@@ -34,7 +34,7 @@ public:
                                 GM_ADDR value_out);
     __aicore__ inline void Process();
     __aicore__ inline void InitParams();
-    __aicore__ inline void GatherKvCache(GlobalTensor<T> dstCacheGm, GlobalTensor<T> srcCacheGm, uint64_t curLen,
+    __aicore__ inline void GatherKvCache(GlobalTensor<T_KEY> dstCacheGm, GlobalTensor<T_KEY> srcCacheGm, uint64_t curLen,
                                          bool isFilledWithZero);
     __aicore__ inline T_INDEX CalcKvCoreOffset(int64_t reduceLen);
 
@@ -45,17 +45,17 @@ private:
     TQue<QuePosition::VECIN, 1> seqLensQueue_;
     TBuf<TPosition::VECCALC> prefixSumBuffer_;
 
-    DataCopyPadExtParams<T> padExtParams_;
+    DataCopyPadExtParams<T_KEY> padExtParams_;
 
     const GatherPaKvCacheTilingDataV35 *tl_;
 
-    GlobalTensor<T> keyCacheGm_;
-    GlobalTensor<T> valueCacheGm_;
+    GlobalTensor<T_KEY> keyCacheGm_;
+    GlobalTensor<T_KEY> valueCacheGm_;
     GlobalTensor<T_INDEX> blockTablesGm_;
     GlobalTensor<T_INDEX> seqLensGm_;
     GlobalTensor<T_INDEX> seqOffsetGm_;
-    GlobalTensor<T> outKeyGm_;
-    GlobalTensor<T> outValueGm_;
+    GlobalTensor<T_KEY> outKeyGm_;
+    GlobalTensor<T_KEY> outValueGm_;
 
     uint32_t batchPerCore_;
     uint32_t needCoreNum_;
@@ -72,29 +72,29 @@ private:
     int64_t numTokens_;
 };
 
-template <typename T, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
-__aicore__ inline void GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffset>::Init(
+template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::Init(
     GM_ADDR key_cache, GM_ADDR value_cache, GM_ADDR block_tables, GM_ADDR seq_lens, GM_ADDR key_in, GM_ADDR value_in,
     GM_ADDR seq_offset, GM_ADDR key_out, GM_ADDR value_out)
 {
     InitParams();
 
-    keyCacheGm_.SetGlobalBuffer((__gm__ T *)(key_cache));
-    valueCacheGm_.SetGlobalBuffer((__gm__ T *)(value_cache));
+    keyCacheGm_.SetGlobalBuffer((__gm__ T_KEY *)(key_cache));
+    valueCacheGm_.SetGlobalBuffer((__gm__ T_KEY *)(value_cache));
     blockTablesGm_.SetGlobalBuffer((__gm__ T_INDEX *)block_tables);
     seqLensGm_.SetGlobalBuffer((__gm__ T_INDEX *)(seq_lens));
     seqOffsetGm_.SetGlobalBuffer((__gm__ T_INDEX *)(seq_offset));
 
-    outKeyGm_.SetGlobalBuffer((__gm__ T *)(key_out));
-    outValueGm_.SetGlobalBuffer((__gm__ T *)(value_out));
+    outKeyGm_.SetGlobalBuffer((__gm__ T_KEY *)(key_out));
+    outValueGm_.SetGlobalBuffer((__gm__ T_KEY *)(value_out));
 
-    pipe_->InitBuffer(cacheQueue_, DOUBLE_BUFFER, (maxUbHiddenSize_) * sizeof(T));
+    pipe_->InitBuffer(cacheQueue_, DOUBLE_BUFFER, (maxUbHiddenSize_) * sizeof(T_KEY));
     pipe_->InitBuffer(seqLensQueue_, DOUBLE_BUFFER, (seqLenAccSize_) * sizeof(T_INDEX));
     pipe_->InitBuffer(prefixSumBuffer_, BLOCK_SIZE);
 }
 
-template <typename T, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
-__aicore__ inline void GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffset>::InitParams()
+template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::InitParams()
 {
     cacheBlockSize_ = tl_->kvCacheBlockSize;
     batchPerCore_ = tl_->batchPerCore;
@@ -117,8 +117,8 @@ __aicore__ inline void GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffs
     padExtParams_.paddingValue = 0;
 }
 
-template <typename T, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
-__aicore__ inline void GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffset>::Process()
+template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::Process()
 {
     int64_t batchStart = GetBlockIdx() * batchPerCore_;
     int64_t batchEnd = batchStart + batchPerCore_;
@@ -218,9 +218,9 @@ __aicore__ inline void GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffs
     }
 }
 
-template <typename T, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
 __aicore__ inline T_INDEX
-GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffset>::CalcKvCoreOffset(int64_t reduceLen)
+GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::CalcKvCoreOffset(int64_t reduceLen)
 {
     LocalTensor<T_INDEX> prefixSumLocal = prefixSumBuffer_.Get<T_INDEX>();
     uint64_t loopTimes = CeilDivision(reduceLen, seqLenAccSize_);
@@ -256,14 +256,14 @@ GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffset>::CalcKvCoreOffset(i
     return coreOffset;
 }
 
-template <typename T, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
-__aicore__ inline void GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffset>::GatherKvCache(
-    GlobalTensor<T> dstCacheGm, GlobalTensor<T> srcCacheGm, uint64_t curLen, bool isFilledWithZero)
+template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::GatherKvCache(
+    GlobalTensor<T_KEY> dstCacheGm, GlobalTensor<T_KEY> srcCacheGm, uint64_t curLen, bool isFilledWithZero)
 {
-    LocalTensor<T> cacheLocal = cacheQueue_.AllocTensor<T>();
+    LocalTensor<T_KEY> cacheLocal = cacheQueue_.AllocTensor<T_KEY>();
     DataCopyExtParams dataCopyParams;
     dataCopyParams.blockCount = 1;
-    dataCopyParams.blockLen = curLen * sizeof(T);
+    dataCopyParams.blockLen = curLen * sizeof(T_KEY);
     dataCopyParams.srcStride = 0;
     dataCopyParams.dstStride = 0;
 
@@ -271,17 +271,17 @@ __aicore__ inline void GatherPaKvCacheNd<T, T_INDEX, isSeqLensCumsum, hasSeqOffs
         AscendC::TEventID eventIdMTE3ToVec = GetTPipePtr()->FetchEventID(HardEvent::MTE3_V);
         SetFlag<HardEvent::MTE3_V>(eventIdMTE3ToVec);
         WaitFlag<HardEvent::MTE3_V>(eventIdMTE3ToVec);
-        Duplicate<T>(cacheLocal, 0, curLen);
+        Duplicate<T_KEY>(cacheLocal, 0, curLen);
         AscendC::TEventID eventIdVecToMTE3 = GetTPipePtr()->FetchEventID(HardEvent::V_MTE3);
         SetFlag<HardEvent::V_MTE3>(eventIdVecToMTE3);
         WaitFlag<HardEvent::V_MTE3>(eventIdVecToMTE3);
     } else {
-        DataCopyPad<T, PaddingMode::Normal>(cacheLocal, srcCacheGm, dataCopyParams, padExtParams_);
-        cacheQueue_.EnQue<T>(cacheLocal);
-        cacheLocal = cacheQueue_.DeQue<T>();
+        DataCopyPad<T_KEY, PaddingMode::Normal>(cacheLocal, srcCacheGm, dataCopyParams, padExtParams_);
+        cacheQueue_.EnQue<T_KEY>(cacheLocal);
+        cacheLocal = cacheQueue_.DeQue<T_KEY>();
     }
 
-    DataCopyPad<T, PaddingMode::Normal>(dstCacheGm, cacheLocal, dataCopyParams);
+    DataCopyPad<T_KEY, PaddingMode::Normal>(dstCacheGm, cacheLocal, dataCopyParams);
     event_t eventIdMTE3ToMTE2 = static_cast<event_t>(GetTPipePtr()->FetchEventID(HardEvent::MTE3_MTE2));
     SetFlag<HardEvent::MTE3_MTE2>(eventIdMTE3ToMTE2);
     WaitFlag<HardEvent::MTE3_MTE2>(eventIdMTE3ToMTE2);
-- 



From 25dbe3c0b0fcb9e541ef7ab43f9978111d8ea486 Mon Sep 17 00:00:00 2001
From: ranzherui <ranzherui@huawei.com>
Date: Tue, 3 Mar 2026 09:00:00 +0800
Subject: [PATCH 2/4] GatherPaKvCacheNd: GatherPaKvCacheNd: declare typename
 TCACHE

---
 .../op_kernel/arch35/gather_pa_kv_cache_nd.h  | 20 ++++++++++---------
 1 file changed, 11 insertions(+), 9 deletions(-)

diff --git a/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h b/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
index 6a8db6b5..22b156d1 100644
--- a/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
+++ b/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
@@ -34,7 +34,8 @@ public:
                                 GM_ADDR value_out);
     __aicore__ inline void Process();
     __aicore__ inline void InitParams();
-    __aicore__ inline void GatherKvCache(GlobalTensor<T_KEY> dstCacheGm, GlobalTensor<T_KEY> srcCacheGm, uint64_t curLen,
+    template <typename T_CACHE>
+    __aicore__ inline void GatherKvCache(GlobalTensor<T_CACHE> dstCacheGm, GlobalTensor<T_CACHE> srcCacheGm, uint64_t curLen,
                                          bool isFilledWithZero);
     __aicore__ inline T_INDEX CalcKvCoreOffset(int64_t reduceLen);
 
@@ -257,13 +258,14 @@ GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::CalcKvCoreOffs
 }
 
 template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+template <typename T_CACHE>
 __aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::GatherKvCache(
-    GlobalTensor<T_KEY> dstCacheGm, GlobalTensor<T_KEY> srcCacheGm, uint64_t curLen, bool isFilledWithZero)
+    GlobalTensor<T_CACHE> dstCacheGm, GlobalTensor<T_CACHE> srcCacheGm, uint64_t curLen, bool isFilledWithZero)
 {
-    LocalTensor<T_KEY> cacheLocal = cacheQueue_.AllocTensor<T_KEY>();
+    LocalTensor<T_CACHE> cacheLocal = cacheQueue_.AllocTensor<T_CACHE>();
     DataCopyExtParams dataCopyParams;
     dataCopyParams.blockCount = 1;
-    dataCopyParams.blockLen = curLen * sizeof(T_KEY);
+    dataCopyParams.blockLen = curLen * sizeof(T_CACHE);
     dataCopyParams.srcStride = 0;
     dataCopyParams.dstStride = 0;
 
@@ -271,17 +273,17 @@ __aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeq
         AscendC::TEventID eventIdMTE3ToVec = GetTPipePtr()->FetchEventID(HardEvent::MTE3_V);
         SetFlag<HardEvent::MTE3_V>(eventIdMTE3ToVec);
         WaitFlag<HardEvent::MTE3_V>(eventIdMTE3ToVec);
-        Duplicate<T_KEY>(cacheLocal, 0, curLen);
+        Duplicate<T_CACHE>(cacheLocal, 0, curLen);
         AscendC::TEventID eventIdVecToMTE3 = GetTPipePtr()->FetchEventID(HardEvent::V_MTE3);
         SetFlag<HardEvent::V_MTE3>(eventIdVecToMTE3);
         WaitFlag<HardEvent::V_MTE3>(eventIdVecToMTE3);
     } else {
-        DataCopyPad<T_KEY, PaddingMode::Normal>(cacheLocal, srcCacheGm, dataCopyParams, padExtParams_);
-        cacheQueue_.EnQue<T_KEY>(cacheLocal);
-        cacheLocal = cacheQueue_.DeQue<T_KEY>();
+        DataCopyPad<T_CACHE, PaddingMode::Normal>(cacheLocal, srcCacheGm, dataCopyParams, padExtParams_);
+        cacheQueue_.EnQue<T_CACHE>(cacheLocal);
+        cacheLocal = cacheQueue_.DeQue<T_CACHE>();
     }
 
-    DataCopyPad<T_KEY, PaddingMode::Normal>(dstCacheGm, cacheLocal, dataCopyParams);
+    DataCopyPad<T_CACHE, PaddingMode::Normal>(dstCacheGm, cacheLocal, dataCopyParams);
     event_t eventIdMTE3ToMTE2 = static_cast<event_t>(GetTPipePtr()->FetchEventID(HardEvent::MTE3_MTE2));
     SetFlag<HardEvent::MTE3_MTE2>(eventIdMTE3ToMTE2);
     WaitFlag<HardEvent::MTE3_MTE2>(eventIdMTE3ToMTE2);
-- 



From 1dc20b8db6c95e0bbcb2e424b914ad4efc8aaf67 Mon Sep 17 00:00:00 2001
From: ranzherui <ranzherui@huawei.com>
Date: Tue, 3 Mar 2026 09:00:00 +0800
Subject: [PATCH 3/4] GatherPaKvCacheNd: declare typename T_VALUE

---
 .../op_kernel/arch35/gather_pa_kv_cache_nd.h  | 46 ++++++++-----------
 .../op_kernel/gather_pa_kv_cache_apt.cpp      |  4 +-
 2 files changed, 20 insertions(+), 30 deletions(-)

diff --git a/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h b/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
index 22b156d1..ff075879 100644
--- a/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
+++ b/attention/gather_pa_kv_cache/op_kernel/arch35/gather_pa_kv_cache_nd.h
@@ -24,7 +24,7 @@ using namespace AscendC;
 constexpr uint32_t BLOCK_SIZE = 32;
 constexpr uint32_t DOUBLE_BUFFER = 2;
 
-template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+template <typename T_KEY, typename T_VALUE, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
 class GatherPaKvCacheNd {
 public:
     __aicore__ inline GatherPaKvCacheNd(TPipe *pipe, const GatherPaKvCacheTilingDataV35 *__restrict tiling)
@@ -46,17 +46,15 @@ private:
     TQue<QuePosition::VECIN, 1> seqLensQueue_;
     TBuf<TPosition::VECCALC> prefixSumBuffer_;
 
-    DataCopyPadExtParams<T_KEY> padExtParams_;
-
     const GatherPaKvCacheTilingDataV35 *tl_;
 
     GlobalTensor<T_KEY> keyCacheGm_;
-    GlobalTensor<T_KEY> valueCacheGm_;
+    GlobalTensor<T_VALUE> valueCacheGm_;
     GlobalTensor<T_INDEX> blockTablesGm_;
     GlobalTensor<T_INDEX> seqLensGm_;
     GlobalTensor<T_INDEX> seqOffsetGm_;
     GlobalTensor<T_KEY> outKeyGm_;
-    GlobalTensor<T_KEY> outValueGm_;
+    GlobalTensor<T_VALUE> outValueGm_;
 
     uint32_t batchPerCore_;
     uint32_t needCoreNum_;
@@ -73,29 +71,29 @@ private:
     int64_t numTokens_;
 };
 
-template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
-__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::Init(
+template <typename T_KEY, typename T_VALUE, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_VALUE, T_INDEX, isSeqLensCumsum, hasSeqOffset>::Init(
     GM_ADDR key_cache, GM_ADDR value_cache, GM_ADDR block_tables, GM_ADDR seq_lens, GM_ADDR key_in, GM_ADDR value_in,
     GM_ADDR seq_offset, GM_ADDR key_out, GM_ADDR value_out)
 {
     InitParams();
 
     keyCacheGm_.SetGlobalBuffer((__gm__ T_KEY *)(key_cache));
-    valueCacheGm_.SetGlobalBuffer((__gm__ T_KEY *)(value_cache));
+    valueCacheGm_.SetGlobalBuffer((__gm__ T_VALUE *)(value_cache));
     blockTablesGm_.SetGlobalBuffer((__gm__ T_INDEX *)block_tables);
     seqLensGm_.SetGlobalBuffer((__gm__ T_INDEX *)(seq_lens));
     seqOffsetGm_.SetGlobalBuffer((__gm__ T_INDEX *)(seq_offset));
 
     outKeyGm_.SetGlobalBuffer((__gm__ T_KEY *)(key_out));
-    outValueGm_.SetGlobalBuffer((__gm__ T_KEY *)(value_out));
+    outValueGm_.SetGlobalBuffer((__gm__ T_VALUE *)(value_out));
 
     pipe_->InitBuffer(cacheQueue_, DOUBLE_BUFFER, (maxUbHiddenSize_) * sizeof(T_KEY));
     pipe_->InitBuffer(seqLensQueue_, DOUBLE_BUFFER, (seqLenAccSize_) * sizeof(T_INDEX));
     pipe_->InitBuffer(prefixSumBuffer_, BLOCK_SIZE);
 }
 
-template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
-__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::InitParams()
+template <typename T_KEY, typename T_VALUE, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_VALUE, T_INDEX, isSeqLensCumsum, hasSeqOffset>::InitParams()
 {
     cacheBlockSize_ = tl_->kvCacheBlockSize;
     batchPerCore_ = tl_->batchPerCore;
@@ -111,15 +109,10 @@ __aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeq
     hiddenSizeK_ = tl_->hiddenSizeK;
     hiddenSizeV_ = tl_->hiddenSizeV;
     numTokens_ = tl_->numTokens;
-
-    padExtParams_.isPad = false;
-    padExtParams_.leftPadding = 0;
-    padExtParams_.rightPadding = 0;
-    padExtParams_.paddingValue = 0;
 }
 
-template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
-__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::Process()
+template <typename T_KEY, typename T_VALUE, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_VALUE, T_INDEX, isSeqLensCumsum, hasSeqOffset>::Process()
 {
     int64_t batchStart = GetBlockIdx() * batchPerCore_;
     int64_t batchEnd = batchStart + batchPerCore_;
@@ -219,9 +212,9 @@ __aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeq
     }
 }
 
-template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+template <typename T_KEY, typename T_VALUE, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
 __aicore__ inline T_INDEX
-GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::CalcKvCoreOffset(int64_t reduceLen)
+GatherPaKvCacheNd<T_KEY, T_VALUE, T_INDEX, isSeqLensCumsum, hasSeqOffset>::CalcKvCoreOffset(int64_t reduceLen)
 {
     LocalTensor<T_INDEX> prefixSumLocal = prefixSumBuffer_.Get<T_INDEX>();
     uint64_t loopTimes = CeilDivision(reduceLen, seqLenAccSize_);
@@ -257,17 +250,14 @@ GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::CalcKvCoreOffs
     return coreOffset;
 }
 
-template <typename T_KEY, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
+template <typename T_KEY, typename T_VALUE, typename T_INDEX, bool isSeqLensCumsum, bool hasSeqOffset>
 template <typename T_CACHE>
-__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeqOffset>::GatherKvCache(
+__aicore__ inline void GatherPaKvCacheNd<T_KEY, T_VALUE, T_INDEX, isSeqLensCumsum, hasSeqOffset>::GatherKvCache(
     GlobalTensor<T_CACHE> dstCacheGm, GlobalTensor<T_CACHE> srcCacheGm, uint64_t curLen, bool isFilledWithZero)
 {
     LocalTensor<T_CACHE> cacheLocal = cacheQueue_.AllocTensor<T_CACHE>();
-    DataCopyExtParams dataCopyParams;
-    dataCopyParams.blockCount = 1;
-    dataCopyParams.blockLen = curLen * sizeof(T_CACHE);
-    dataCopyParams.srcStride = 0;
-    dataCopyParams.dstStride = 0;
+    DataCopyExtParams dataCopyParams{1, static_cast<uint32_t>(curLen * sizeof(T_CACHE)), 0, 0, 0};
+    DataCopyPadExtParams<T_CACHE> padParams{};
 
     if (isFilledWithZero) {
         AscendC::TEventID eventIdMTE3ToVec = GetTPipePtr()->FetchEventID(HardEvent::MTE3_V);
@@ -278,7 +268,7 @@ __aicore__ inline void GatherPaKvCacheNd<T_KEY, T_INDEX, isSeqLensCumsum, hasSeq
         SetFlag<HardEvent::V_MTE3>(eventIdVecToMTE3);
         WaitFlag<HardEvent::V_MTE3>(eventIdVecToMTE3);
     } else {
-        DataCopyPad<T_CACHE, PaddingMode::Normal>(cacheLocal, srcCacheGm, dataCopyParams, padExtParams_);
+        DataCopyPad<T_CACHE, PaddingMode::Normal>(cacheLocal, srcCacheGm, dataCopyParams, padParams);
         cacheQueue_.EnQue<T_CACHE>(cacheLocal);
         cacheLocal = cacheQueue_.DeQue<T_CACHE>();
     }
diff --git a/attention/gather_pa_kv_cache/op_kernel/gather_pa_kv_cache_apt.cpp b/attention/gather_pa_kv_cache/op_kernel/gather_pa_kv_cache_apt.cpp
index 030543b9..74643c56 100644
--- a/attention/gather_pa_kv_cache/op_kernel/gather_pa_kv_cache_apt.cpp
+++ b/attention/gather_pa_kv_cache/op_kernel/gather_pa_kv_cache_apt.cpp
@@ -39,11 +39,11 @@ extern "C" __global__ __aicore__ void gather_pa_kv_cache(GM_ADDR key_cache, GM_A
             if constexpr (AscendC::IsSameType<DTYPE_KEY_CACHE, hifloat8_t>::value ||                                                            \
                           AscendC::IsSameType<DTYPE_KEY_CACHE, fp8_e5m2_t>::value ||                                                            \
                           AscendC::IsSameType<DTYPE_KEY_CACHE, fp8_e4m3fn_t>::value) {                                                          \
-                GatherPaKvCacheV35::GatherPaKvCacheNd<uint8_t, DTYPE_SEQ_LENS, isSeqLenCumSum, hasSeqOffset> op(&pipe, &tilingData);            \
+                GatherPaKvCacheV35::GatherPaKvCacheNd<uint8_t, uint8_t, DTYPE_SEQ_LENS, isSeqLenCumSum, hasSeqOffset> op(&pipe, &tilingData);            \
                 op.Init(key_cache, value_cache, block_tables, seq_lens, key_in, value_in, seq_offset, key_out, value_out);                      \
                 op.Process();                                                                                                                   \
             } else {                                                                                                                            \
-                GatherPaKvCacheV35::GatherPaKvCacheNd<DTYPE_KEY_CACHE, DTYPE_SEQ_LENS, isSeqLenCumSum, hasSeqOffset> op(&pipe, &tilingData);    \
+                GatherPaKvCacheV35::GatherPaKvCacheNd<DTYPE_KEY_CACHE, DTYPE_KEY_CACHE, DTYPE_SEQ_LENS, isSeqLenCumSum, hasSeqOffset> op(&pipe, &tilingData);    \
                 op.Init(key_cache, value_cache, block_tables, seq_lens, key_in, value_in, seq_offset, key_out, value_out);                      \
                 op.Process();                                                                                                                   \
             }                                                                                                                                   \
-- 



From d923c54776d40360ead90ee1da3610450a1f541b Mon Sep 17 00:00:00 2001
From: ranzherui <ranzherui@huawei.com>
Date: Tue, 3 Mar 2026 11:00:00 +0800
Subject: [PATCH 4/4] gather_pa_kv_cache: GatherPaKvCacheNd: use specification
 from DTYPE_VALUE_CACHE

---
 .../op_kernel/gather_pa_kv_cache_apt.cpp      | 30 ++++++++++++-------
 1 file changed, 19 insertions(+), 11 deletions(-)

diff --git a/attention/gather_pa_kv_cache/op_kernel/gather_pa_kv_cache_apt.cpp b/attention/gather_pa_kv_cache/op_kernel/gather_pa_kv_cache_apt.cpp
index 74643c56..fe237cc3 100644
--- a/attention/gather_pa_kv_cache/op_kernel/gather_pa_kv_cache_apt.cpp
+++ b/attention/gather_pa_kv_cache/op_kernel/gather_pa_kv_cache_apt.cpp
@@ -25,28 +25,36 @@
 #define TILING_KEY_1001 1001
 #define TILING_KEY_1000 1000
 
+template <bool B, class T, class F>
+struct Conditional {
+    typedef T type;
+};
+
+template <class T, class F>
+struct Conditional<false, T, F> {
+    typedef F type;
+};
+
 extern "C" __global__ __aicore__ void gather_pa_kv_cache(GM_ADDR key_cache, GM_ADDR value_cache,
                                                          GM_ADDR block_tables, GM_ADDR seq_lens,
                                                          GM_ADDR key_in, GM_ADDR value_in, GM_ADDR seq_offset,
                                                          GM_ADDR key_out, GM_ADDR value_out,
                                                          GM_ADDR workspace, GM_ADDR tiling)
 {
+    using T_KEY = Conditional<AscendC::IsSameType<DTYPE_KEY_CACHE, fp8_e4m3fn_t>::value ||
+        AscendC::IsSameType<DTYPE_KEY_CACHE, fp8_e5m2_t>::value ||
+        AscendC::IsSameType<DTYPE_KEY_CACHE, hifloat8_t>::value, uint8_t, DTYPE_KEY_CACHE>::type;
+    using T_VALUE = Conditional<AscendC::IsSameType<DTYPE_VALUE_CACHE, fp8_e4m3fn_t>::value ||
+        AscendC::IsSameType<DTYPE_VALUE_CACHE, fp8_e5m2_t>::value ||
+        AscendC::IsSameType<DTYPE_VALUE_CACHE, hifloat8_t>::value, uint8_t, DTYPE_VALUE_CACHE>::type;
 // 宏封装，简化分支代码
 // B8几种类型编译DataCopyPad报错，用uint8_t代替规避
 #define TILING_KEY_BRANCH(tilingKey, isCacheModeNorm, isSeqLenCumSum, hasSeqOffset) {                                                           \
     if (TILING_KEY_IS(tilingKey)) {                                                                                                             \
         if constexpr (isCacheModeNorm == true) {                                                                                                \
-            if constexpr (AscendC::IsSameType<DTYPE_KEY_CACHE, hifloat8_t>::value ||                                                            \
-                          AscendC::IsSameType<DTYPE_KEY_CACHE, fp8_e5m2_t>::value ||                                                            \
-                          AscendC::IsSameType<DTYPE_KEY_CACHE, fp8_e4m3fn_t>::value) {                                                          \
-                GatherPaKvCacheV35::GatherPaKvCacheNd<uint8_t, uint8_t, DTYPE_SEQ_LENS, isSeqLenCumSum, hasSeqOffset> op(&pipe, &tilingData);            \
-                op.Init(key_cache, value_cache, block_tables, seq_lens, key_in, value_in, seq_offset, key_out, value_out);                      \
-                op.Process();                                                                                                                   \
-            } else {                                                                                                                            \
-                GatherPaKvCacheV35::GatherPaKvCacheNd<DTYPE_KEY_CACHE, DTYPE_KEY_CACHE, DTYPE_SEQ_LENS, isSeqLenCumSum, hasSeqOffset> op(&pipe, &tilingData);    \
-                op.Init(key_cache, value_cache, block_tables, seq_lens, key_in, value_in, seq_offset, key_out, value_out);                      \
-                op.Process();                                                                                                                   \
-            }                                                                                                                                   \
+            GatherPaKvCacheV35::GatherPaKvCacheNd<T_KEY, T_VALUE, DTYPE_SEQ_LENS, isSeqLenCumSum, hasSeqOffset> op(&pipe, &tilingData);         \
+            op.Init(key_cache, value_cache, block_tables, seq_lens, key_in, value_in, seq_offset, key_out, value_out);                          \
+            op.Process();                                                                                                                       \
         } else {                                                                                                                                \
             if constexpr (AscendC::IsSameType<DTYPE_KEY_CACHE, hifloat8_t>::value ||                                                            \
                           AscendC::IsSameType<DTYPE_KEY_CACHE, fp8_e5m2_t>::value ||                                                            \
-- 


