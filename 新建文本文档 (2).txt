matmul_bias.cpp (Host入口)
    ↓
DeviceGemm (设备层封装)
    ↓
KernelAdapter (核函数启动器)
    ↓
MatmulBias (核函数实现)
    ↓
BlockScheduler (块调度器)
    ↓
BlockMmad (块级矩阵乘法)
    ↓
TileMmad (Tile级矩阵乘法)

┌─────────────────────────────────────────────────────────────┐
│ 1. matmul_bias.cpp (Host)                                    │
│    - 初始化ACL环境                                           │
│    - 分配GM内存                                              │
│    - 配置矩阵参数                                            │
│    - 实例化DeviceGemm并调用Run()                             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. device_gemm.hpp (Device Layer)                            │
│    - CanImplement(): 检查问题可行性                          │
│    - GetWorkspaceSize(): 计算工作空间                        │
│    - Initialize(): 初始化参数                                │
│    - Run(): 启动核函数                                       │
│      └─> KernelAdapter<MatmulBiasKernel><<<...>>>()          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. kernel_adapter.hpp (Kernel Adapter)                       │
│    - SetSyncBaseAddr(): 设置同步地址                         │
│    - 实例化MatmulBias对象                                    │
│    - 调用op(params)                                          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. matmul_bias.hpp (Kernel Layer)                            │
│    operator()(params):                                        │
│    - 初始化BlockScheduler                                    │
│    - 循环处理所有块:                                          │
│      ├─> scheduler.GetBlockCoord(taskIdx)                    │
│      ├─> 计算GM地址偏移                                       │
│      └─> block_mmad(gmA, gmB, gmBias, gmC, ...)              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. block_swizzle.hpp (Block Scheduler)                       │
│    - GetBlockCoord(): 计算块坐标（支持Swizzle优化）          │
│    - GetActualBlockShape(): 获取实际块形状                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. block_mmad_pingpong_bias.hpp (Block Compute)              │
│    operator()(...):                                           │
│    - 初始化L1/L0缓冲区                                        │
│    - 预加载数据到L1                                           │
│    - 主循环（四重循环）:                                       │
│      ├─> WaitFlag(): 等待L1数据就绪                          │
│      ├─> CopyL1ToL0(): 拷贝到L0                               │
│      ├─> CopyGmToL1(): 预取下一块（Ping-Pong）                │
│      ├─> tileMmad(): 执行Tile矩阵乘法                        │
│      └─> CopyL0CToGm(): 回写结果                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. tile_mmad.hpp (Tile Compute)                              │
│    operator()(...):                                           │
│    - 配置Mmad参数                                             │
│    - AscendC::Mmad(): 执行硬件矩阵乘法指令                   │
│    - PipeBarrier(): 流水线屏障                               │
└─────────────────────────────────────────────────────────────┘