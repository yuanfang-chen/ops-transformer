name: zta5 build check

on:
  workflow_dispatch:
  push:
    branches:
      - claude/check-gh-runners-Ux4Ti
    paths:
      - .github/workflows/zta5-build-check.yml

concurrency:
  group: runner-singleton-zta5
  cancel-in-progress: false

defaults:
  run:
    shell: bash -el {0}

jobs:
  build:
    runs-on: [self-hosted, Linux, X64, zta5]
    timeout-minutes: 120
    steps:
      - name: sync container to dispatched SHA
        run: |
          set -x
          source ~/test_runner.bash
          for attempt in 1 2 3 4 5; do
            if zta5runner git fetch --progress --force --prune origin "${GITHUB_REF}"; then
              break
            fi
            if [ "$attempt" = "5" ]; then
              echo "git fetch failed after 5 attempts"
              exit 1
            fi
            base=$((5 * (2 ** (attempt - 1))))
            delay=$((base + (RANDOM % base)))
            sleep "$delay"
          done
          zta5runner git reset --hard "${GITHUB_SHA}"

      - name: build (ophost, ascend950)
        id: build
        run: |
          set -o pipefail
          source ~/test_runner.bash
          zta5runner bash -c 'rm -f /tmp/zta5_build.log; bash build.sh -u --ophost --soc=ascend950 -j16 2>&1 | tee /tmp/zta5_build.log; exit "${PIPESTATUS[0]}"'

      - name: dump build log tail
        if: always()
        run: |
          source ~/test_runner.bash
          echo "===== last 200 lines of /tmp/zta5_build.log ====="
          zta5runner tail -n 200 /tmp/zta5_build.log || true
